version: "2"

linters:
  enable:
    - errcheck
    - gosec
    - staticcheck
  settings:
    gosec:
      excludes:
        - G115  # integer overflow on conversion — proto int64 fields
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck
